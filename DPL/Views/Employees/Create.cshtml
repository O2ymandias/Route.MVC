@model EmployeeVm
@{
	ViewData["Title"] = "Create";
}

<h1>Create New Employee</h1>

<form class="mt-3" asp-action="Create" method="post" enctype="multipart/form-data">
	@*
	application/x-www-form-urlencoded (DEFAULT):
	- Data is encoded as key-value pairs, with keys and values being URL-encoded.
	- Spaces are replaced by + symbols, and special characters are encoded as %HH (hexadecimal).
	- Suitable for most forms.
	- Request body will be like: username=JohnDoe&password=12345 (KVP)

	multipart/form-data:
	- Used when a form includes file uploads.
	- Each form field is sent as a separate part of a multi-part message.
	- This encoding type is necessary for file uploads.
	- Request headers will be like:
	Content-Disposition: form-data; name="Image"; filename="example.jpg"
	Content-Type: image/jpeg

	text/plain:
	- Data is encoded in plain text, with no encoding applied.
	- Not commonly used, as it is not suitable for most form submissions.
	- Request body will be like:
	username=JohnDoe
	password=12345
	*@

	<div class="text-danger" asp-validation-summary="All"></div>
	@*
	The asp-validation-summary="All":
	will display both model-level and property-level errors.
	asp-validation-summary="ModelOnly":
	will only show the model-level errors added with ModelState.AddModelError("", "Error message").
	*@

	<partial name="PartialViews/CreateEditPartial" model="@Model" />
	<br />
	<div>
		<input class="btn btn-success" type="submit" value="Create" />|
		<a class="btn btn-light" asp-action="Index">Back To List</a>
	</div>

</form>

@section Scripts {
	<partial name="_ValidationScriptsPartial" />
}

